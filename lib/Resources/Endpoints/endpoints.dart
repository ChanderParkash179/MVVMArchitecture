class Endpoints {
  static var baseEndpoint = 'https://reqres.in';

  static var loginEndpoint = baseEndpoint + '/api/login';

  static var registerEndpoint = baseEndpoint + '/api/register';

  static var moviesEndpoint = 'https://mcuapi.herokuapp.com/api/v1/movies';
}
